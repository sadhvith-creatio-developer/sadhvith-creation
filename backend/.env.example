# ---- MongoDB Atlas ----
# Create a free cluster at https://www.mongodb.com/cloud/atlas, add a
# database user, allow your IP (or 0.0.0.0/0 for development), then copy
# the connection string below.
MONGODB_URI=mongodb+srv://amsubbaraya2029_db_user:glhdlefpxBIkKRBQ@sadhvith-creation.ksstnqg.mongodb.net/?appName=sadhvith-creation
MONGODB_DB_NAME=sadhvith_creation

# ---- JWT ----
# Any long random string. Generate one with: openssl rand -hex 32
JWT_SECRET=replace_with_a_long_random_secret
JWT_ALGORITHM=HS256
JWT_EXPIRE_MINUTES=1440

# ---- Cloudinary ----
# From your Cloudinary dashboard: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME=qnx2rzbz
CLOUDINARY_API_KEY=491535877181793
CLOUDINARY_API_SECRET=AHkMaVvO3WWtx24WqaMXHwCF05I

# ---- Frontend origins (CORS) ----
# Customer storefront (Vercel project #1)
FRONTEND_URL=http://localhost:5173
# Manager / uploader dashboard (Vercel project #2 — a separate deployment)
MANAGER_FRONTEND_URL=http://localhost:5174
# Admin dashboard — full access (Vercel project #3 — a separate deployment)
ADMIN_FRONTEND_URL=http://localhost:5175

# ---- Email (SMTP) — sends OTP codes for registration + password reset ----
# Example for Gmail: host=smtp.gmail.com, port=587, use an App Password
# (not your normal password) as SMTP_PASSWORD.
# Leave SMTP_HOST empty during local development — OTP codes will be
# printed to the backend console/log instead of emailed.
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USERNAME=aicontentcreator772@gmail.com
SMTP_PASSWORD=qvkj ssnb qjrj hrbl
SMTP_USE_TLS=true
SMTP_FROM_EMAIL=aicontentcreator772@gmail.com
SMTP_FROM_NAME=SADHVITH CREATION

# ---- OTP (email verification + forgot password) ----
OTP_EXPIRE_MINUTES=10
OTP_MAX_ATTEMPTS=5
OTP_RESEND_COOLDOWN_SECONDS=45

# ---- Server ----
PORT=8000
ENV=development

# ---- WhatsApp ----
# International format, digits only, no + or leading zeros.
WHATSAPP_NUMBER=918310611507

# ---- Uploads ----
MAX_IMAGE_SIZE_MB=5
